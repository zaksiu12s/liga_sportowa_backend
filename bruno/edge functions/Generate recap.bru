meta {
  name: Generate recap
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/functions/v1/report-generator
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

body:json {
  {
    "provider": "groq",
    "request_type": "recap"
  }
}